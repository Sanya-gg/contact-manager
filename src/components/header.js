import React from 'react';

const Header = () => {
    return (
        <div className="ui fixed menu" style={{ justifyContent: 'center', alignItems: 'center', display: 'flex', height: '60px' }}>
            <h2 style={{ margin: 0 }}>Contact Manager</h2>
        </div>
    );
};

export default Header;
